# ==============================================================================
# ContextTree MCP — runtime dependencies (pip compatibility layer)
#
# Canonical source of dependencies and tooling config: pyproject.toml
# (uv-managed, locked via uv.lock). This file exists only for environments that
# prefer plain `pip install -r requirements.txt`.
#
# Target: Python 3.12+
#
# NOTE: `sentence-transformers` pulls in PyTorch as a transitive dependency
#       (~2 GB download on first install). The CPU build is sufficient.
# ==============================================================================

# --- MCP protocol: official Python SDK v2 (stdio transport) ---
mcp[cli]>=2.0.0

# --- AST parsing: tree-sitter core + language bindings ---
tree-sitter>=0.26.0,<1.0
tree-sitter-python>=0.25.0
tree-sitter-typescript>=0.23.2   # ships both `typescript` and `tsx` grammars
tree-sitter-javascript>=0.25.0
tree-sitter-go>=0.25.0
tree-sitter-rust>=0.24.2
tree-sitter-c-sharp>=0.23.5
tree-sitter-java>=0.23.5
tree-sitter-c>=0.24.2
tree-sitter-cpp>=0.23.4
tree-sitter-kotlin>=1.1.0
tree-sitter-swift>=0.7.3

# --- Vector store & embeddings (100% local) ---
chromadb>=1.5.9
sentence-transformers>=6.0.0

# --- File system watcher (watch mode) ---
watchfiles>=1.0.0
